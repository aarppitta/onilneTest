{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": { "distDir": "dist/online-test" }
      }
    ],
    "routes": [
      {
        "src": "/api/.*",
        "dest": "/api/db.json"
      }
    ],
    "env": {
      "CUSTOM_VARIABLE": "value"
    }
  }